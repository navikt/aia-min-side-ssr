name: "Deploy dev&prod"
on:
  push:
    branches:
      - "main"
      - "dev/*"

jobs:
  build:
    name: "test & build"
    runs-on: "ubuntu-latest"

    permissions:
      contents: "read"
      id-token: "write"
      packages: "write"

    steps:
      - uses: "actions/checkout@v4"
      - uses: "actions/setup-node@v4"
        with:
          node-version: 22
          registry-url: "https://npm.pkg.github.com"
          cache: "npm"

      - name: "Install dependencies"
        run: "npm ci"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.READER_TOKEN }}

      - name: "Install playwright"
        run: "npx playwright install"

      - name: "Run tests"
        run: "npm run test:ci"

      - name: "Build application"
        run: "npm run build"
        env:
          ASTRO_KEY: ${{ secrets.ASTRO_KEY }}

      - name: "Build storybook"
        run: "npm run build-storybook"

      - name: "Upload to cdn"
        uses: nais/deploy/actions/cdn-upload/v2@master
        with:
          team: paw
          source: ./dist/client/_astro
          destination: 'aia-min-side-ssr' # Change this to your application name
          identity_provider: ${{ secrets.NAIS_WORKLOAD_IDENTITY_PROVIDER }}
          project_id: ${{ vars.NAIS_MANAGEMENT_PROJECT_ID }}

      - name: "Build and push"
        uses: nais/docker-build-push@v0
        id: docker-build-push
        with:
          team: paw

    outputs:
      image: ${{ steps.docker-build-push.outputs.image }}

  update-manifest-dev:
    uses: navikt/tms-deploy/.github/workflows/oppdater-mikrofrontend-manifest-v3.yaml@main
    needs: build
    with:
      id: "aia-min-side-ssr" # Change this to your application name
      url: "http://aia-min-side-ssr" # Change the url to contain your application name. Example: http://my-app
      appname: "aia-min-side-ssr" # Change this to your application name
      namespace: "paw" # Change this to your namespace
      cluster: dev-gcp
      commitmsg: ${{ github.event.head_commit.message}}
      fallback: "fallback" # Should containt the path to the fallback page after src/pages/[locale]/... Only needs to be changed if you move the fallback in the pages structure
      ssr: true
    secrets: inherit

  # update-manifest-prod:
  #   if: github.ref == 'refs/heads/main'
  #   uses: navikt/tms-deploy/.github/workflows/oppdater-mikrofrontend-manifest-v3.yaml@main
  #   needs: build
  #   with:
  #     id: "aia-min-side-ssr-id"
  #     url: "http://aia-min-side-ssr.min-side"
  #     appname: "aia-min-side-ssr"
  #     namespace: "team-namespace"
  #     cluster: prod-gcp
  #     commitmsg: ${{ github.event.head_commit.message}}
  #     fallback: "fallback" # Should containt the path to the fallback page after src/pages/[locale]/...
  #     ssr: true
  #   secrets: inherit

  deploy-dev:
    runs-on: "ubuntu-latest"
    permissions:
      contents: "read"
      id-token: "write"
    needs: [build, update-manifest-dev]
    steps:
      - uses: "actions/checkout@v4"
      - name: "Deploy to dev"
        uses: "nais/deploy/actions/deploy@v2"
        env:
          CLUSTER: dev-gcp
          RESOURCE: nais/dev-gcp/nais.yaml
          VAR: image=${{ needs.build.outputs.image }},version=${{ github.sha }}

  # deploy-prod:
  #   if: github.ref == 'refs/heads/main'
  #   runs-on: "ubuntu-latest"
  #   permissions:
  #     contents: "read"
  #     id-token: "write"
  #   needs: [build, update-manifest-prod]

  #   steps:
  #     - uses: "actions/checkout@v4"
  #     - name: "Deploy to prod"
  #       uses: "nais/deploy/actions/deploy@v2"
  #       env:
  #         CLUSTER: prod-gcp
  #         RESOURCE: nais/prod-gcp/nais.yaml
  #         VAR: image=${{ needs.build.outputs.image }},version=${{ github.sha }}
