import { Canvas, Meta } from '@storybook/addon-docs/blocks';

import * as FeilStories from './feil.stories.tsx';

<Meta of={FeilStories} />

# Feilmelding

Feilmelding som vises hvis en uventet feil kastes et sted i applikasjonen.
Feil fakkes og logges til umami, mens feilmelding vises som fallback.

<Canvas of={FeilStories.ErrorBoundaryFeilKomponent} />
