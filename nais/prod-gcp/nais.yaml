apiVersion: "nais.io/v1alpha1"
kind: "Application"
metadata:
  name: aia-min-side-ssr
  namespace: paw
  labels:
    team: paw
spec:
  observability:
    autoInstrumentation:
      enabled: true
      runtime: nodejs
  tokenx:
    enabled: true
  image: {{ image }}
  port: 3000
  liveness:
    path: /api/internal/isAlive
    initialDelay: 10
  readiness:
    path: /api/internal/isReady
    initialDelay: 10
  replicas:
    min: 2
    max: 4
  accessPolicy:
    inbound:
      rules:
        - application: tms-min-side
          namespace: min-side
    outbound:
      rules:
        - application: paw-arbeidssoekerregisteret-api-oppslag-v2
        - application: paw-arbeidssoekerregisteret-api-bekreftelse
  resources:
    limits:
      memory: 768Mi
    requests:
      cpu: "20m"
      memory: 128Mi
  env: # Add your environment variables here
    - name: SIDE_2_URL
      value: https://www.nav.no/arbeidssoekerregisteret
    - name: ARBEIDSSOEKERPERIODER_SNAPSHOT_URL
      value: http://paw-arbeidssoekerregisteret-api-oppslag-v2/api/v3/snapshot
    - name:  ARBEIDSSOKERREGISTERET_OPPSLAG_CLIENT_ID
      value: prod-gcp:paw:paw-arbeidssoekerregisteret-api-oppslag-v2
    - name: TILGJENGELIGE_BEKREFTELSER_URL
      value: http://paw-arbeidssoekerregisteret-api-bekreftelse/api/v1/tilgjengelige-bekreftelser
    - name: BEKREFTELSE_API_CLIENT_ID
      value: prod-gcp:paw:paw-arbeidssoekerregisteret-api-bekreftelse
